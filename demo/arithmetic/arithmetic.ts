/* eslint-disable */
import { strict as assert } from "assert";

const add = 1 + 1;
assert.equal(add, 2);

const sub = 2 - 1;
assert.equal(sub, 1);

const mul = 3 * 3;
assert.equal(mul, 9);

const div = 8 / 2;
assert.equal(div, 4);